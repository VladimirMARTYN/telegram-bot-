# 📚 Пошаговая настройка AI-дайджестов

## 🎯 Что делает система дайджестов?

Система автоматически:
- 📥 Мониторит новости из указанных Telegram-каналов
- 🔍 Находит и удаляет дубликаты с помощью векторного поиска
- 🎯 Группирует новости по темам с помощью AI
- 📝 Создает краткие резюме для каждой темы
- 📤 Публикует структурированные дайджесты по расписанию

---

## 📋 Шаг 1: Получение необходимых API ключей

### 1.1 Telegram API (обязательно)

**Для чего:** Мониторинг каналов и сбор новостей

**Как получить:**
1. Перейдите на https://my.telegram.org/
2. Войдите с вашим номером телефона (например: +79991234567)
3. Введите код из Telegram
4. Перейдите в раздел **"API development tools"**
5. Создайте приложение:
   - **App title:** любое название (например: "Digest Bot")
   - **Short name:** любое короткое имя
   - **Platform:** Desktop
6. Скопируйте:
   - **api_id** (число, например: `12345678`)
   - **api_hash** (строка, например: `abcdef1234567890abcdef1234567890`)

⚠️ **Важно:** Эти данные нужны для авторизации Telethon (клиент Telegram)

---

### 1.2 Qdrant (обязательно)

**Для чего:** Векторная база данных для поиска дубликатов новостей

#### Вариант A: Облачный Qdrant (рекомендуется)

1. Зарегистрируйтесь на https://cloud.qdrant.io/
2. Создайте бесплатный кластер:
   - Нажмите "Create Cluster"
   - Выберите бесплатный план (Free tier)
   - Выберите регион (ближайший к вам)
3. После создания кластера:
   - Скопируйте **URL** (например: `https://xxxxx-xxxxx.qdrant.io`)
   - Перейдите в "API Keys" → "Create API Key"
   - Скопируйте **API Key**

#### Вариант B: Локальный Qdrant (для тестирования)

```bash
docker run -p 6333:6333 qdrant/qdrant
```

- URL: `http://localhost:6333`
- API Key: оставьте пустым

---

### 1.3 OpenAI API (обязательно)

**Для чего:** Создание эмбеддингов и суммаризация новостей

**Проверьте:**
- У вас уже должен быть `OPENAI_API_KEY` в настройках Railway
- Если нет, получите на https://platform.openai.com/api-keys

---

## 📝 Шаг 2: Настройка переменных окружения на Railway

### 2.1 Откройте настройки проекта на Railway

1. Перейдите на https://railway.app
2. Выберите ваш проект
3. Перейдите в **"Variables"** (или **"Environment"**)

### 2.2 Добавьте следующие переменные:

```env
# ============================================
# ВКЛЮЧЕНИЕ СИСТЕМЫ ДАЙДЖЕСТОВ
# ============================================
DIGEST_ENABLED=true

# ============================================
# TELEGRAM API (из шага 1.1)
# ============================================
TELEGRAM_API_ID=ваш_api_id_число
TELEGRAM_API_HASH=ваш_api_hash_строка

# ============================================
# QDRANT (из шага 1.2)
# ============================================
QDRANT_URL=https://ваш-кластер.qdrant.io
QDRANT_API_KEY=ваш_qdrant_api_key

# ============================================
# КАНАЛЫ ДЛЯ МОНИТОРИНГА
# ============================================
# Укажите каналы через запятую (с @ или без)
# Примеры:
# DIGEST_SOURCE_CHANNELS=@channel1,@channel2,@channel3
# DIGEST_SOURCE_CHANNELS=channel1,channel2
DIGEST_SOURCE_CHANNELS=@ваш_канал1,@ваш_канал2

# ============================================
# КАНАЛ ДЛЯ ПУБЛИКАЦИИ ДАЙДЖЕСТОВ
# ============================================
# Может быть:
# - Ваш личный канал: @your_digest_channel
# - Ваш бот (для личных сообщений): оставьте пустым или укажите ваш Telegram ID
DIGEST_DEST_CHANNEL=@ваш_канал_для_дайджестов

# ============================================
# НАСТРОЙКИ (опционально)
# ============================================
# Имя коллекции в Qdrant (по умолчанию: telegram_digest)
DIGEST_COLLECTION_NAME=telegram_digest

# Порог схожести для дубликатов (0.0-1.0, по умолчанию: 0.85)
# Чем выше, тем строже фильтрация дубликатов
DIGEST_SIMILARITY_THRESHOLD=0.85

# Модель для эмбеддингов (по умолчанию: text-embedding-3-small)
DIGEST_EMBEDDING_MODEL=text-embedding-3-small

# Модель для суммаризации (по умолчанию: gpt-4o-mini)
# Варианты: gpt-4o-mini, gpt-4o, gpt-3.5-turbo
DIGEST_LLM_MODEL=gpt-4o-mini

# Расписание публикации (hourly/daily/weekly, по умолчанию: daily)
DIGEST_PUBLISH_SCHEDULE=daily
```

---

## 🔧 Шаг 3: Первый запуск и авторизация

### 3.1 После добавления переменных

Railway автоматически перезапустит бота. Дождитесь завершения деплоя.

### 3.2 Авторизация в Telegram

При первом запуске с включенными дайджестами Telethon попросит авторизоваться.

**Как это работает:**
1. Бот создаст сессию Telethon
2. В логах Railway вы увидите запрос на авторизацию
3. **НО:** На Railway нет интерактивного ввода!

**Решение:** Авторизация должна произойти автоматически через переменные окружения или нужно использовать другой подход.

**Альтернативный способ авторизации:**

1. **Локально:**
   ```bash
   # Создайте файл .env с вашими ключами
   TELEGRAM_API_ID=ваш_id
   TELEGRAM_API_HASH=ваш_hash
   
   # Запустите скрипт авторизации
   python3 -c "
   from telethon import TelegramClient
   import asyncio
   import os
   from dotenv import load_dotenv
   load_dotenv()
   
   async def auth():
       client = TelegramClient('digest_bot_session', 
                              int(os.getenv('TELEGRAM_API_ID')), 
                              os.getenv('TELEGRAM_API_HASH'))
       await client.start()
       print('Авторизация успешна!')
       await client.disconnect()
   
   asyncio.run(auth())
   "
   ```

2. **Загрузите файл сессии на Railway:**
   - После авторизации создастся файл `digest_bot_session.session`
   - Загрузите его в Railway через переменную окружения или как файл

**Или используйте готовую сессию:**
- Если у вас уже есть авторизованный Telethon клиент, скопируйте файл `.session`

---

## ✅ Шаг 4: Проверка работы

### 4.1 Проверка статуса

В боте выполните команду (только для админа):
```
/digest_status
```

**Ожидаемый результат:**
```
✅ Система дайджестов: Включена

📥 Каналы для мониторинга:
- @channel1
- @channel2

📤 Канал публикации: @your_digest_channel

📊 Статус:
- Новостей в буфере: 0
- Обработано сообщений: 0
- Telegram клиент: ✅ Инициализирован
- Qdrant: ✅ Подключен
```

### 4.2 Загрузка последних новостей

```
/digest_load 24
```

Это загрузит новости за последние 24 часа из указанных каналов.

**Ожидаемый результат:**
```
📥 Загрузка новостей за последние 24 часа...

✅ Загружено новостей: 15
✅ Обработано: 12 (3 дубликата отфильтровано)
```

### 4.3 Создание тестового дайджеста

```
/digest_create
```

Это создаст и опубликует дайджест из накопленных новостей.

**Ожидаемый результат:**
```
📝 Создание дайджеста...

✅ Дайджест создан и опубликован!
📊 Обработано новостей: 12
📁 Групп по темам: 3
```

---

## ⚙️ Шаг 5: Настройка каналов

### 5.1 Каналы для мониторинга

**Публичные каналы:**
- Просто укажите `@channel_name` в `DIGEST_SOURCE_CHANNELS`
- Бот должен быть подписан на канал (или канал должен быть публичным)

**Приватные каналы:**
- Добавьте бота как участника канала
- Укажите имя канала без @ (например: `channel_name`)

**Пример:**
```env
DIGEST_SOURCE_CHANNELS=@tech_news,@finance_news,@crypto_updates
```

### 5.2 Канал для публикации

**Вариант 1: Публикация в канал**
```env
DIGEST_DEST_CHANNEL=@your_digest_channel
```
- Бот должен быть администратором канала
- Бот должен иметь право публиковать сообщения

**Вариант 2: Публикация в личные сообщения**
```env
DIGEST_DEST_CHANNEL=
```
или
```env
DIGEST_DEST_CHANNEL=ваш_telegram_id
```
- Дайджест будет отправляться вам в личные сообщения

---

## ⏰ Шаг 6: Настройка расписания

### Варианты расписания:

**Ежечасно:**
```env
DIGEST_PUBLISH_SCHEDULE=hourly
```
- Дайджест публикуется каждый час

**Ежедневно (по умолчанию):**
```env
DIGEST_PUBLISH_SCHEDULE=daily
```
- Дайджест публикуется ежедневно в 09:00 МСК

**Еженедельно:**
```env
DIGEST_PUBLISH_SCHEDULE=weekly
```
- Дайджест публикуется еженедельно по понедельникам в 09:00 МСК

---

## 💰 Шаг 7: Понимание расходов OpenAI

### Примерные расходы:

**Эмбеддинги (text-embedding-3-small):**
- ~$0.0001 за 1000 токенов
- Одно сообщение ≈ 50-200 токенов
- **Стоимость:** ~$0.00001-0.00002 за сообщение

**Суммаризация (gpt-4o-mini):**
- ~$0.00015 за 1000 входных токенов
- ~$0.0006 за 1000 выходных токенов
- Один дайджест ≈ 2000-5000 токенов
- **Стоимость:** ~$0.001-0.003 за дайджест

**Пример расчета:**
- 100 новостей в день
- Эмбеддинги: 100 × $0.00001 = $0.001
- Дайджест: $0.002
- **Итого:** ~$0.003/день = ~$0.09/месяц

---

## 🆘 Решение проблем

### Проблема: "Telegram client not initialized"

**Причина:** Не удалось авторизоваться в Telegram

**Решение:**
1. Проверьте `TELEGRAM_API_ID` и `TELEGRAM_API_HASH`
2. Убедитесь, что они правильные (без пробелов, кавычек)
3. Попробуйте авторизоваться локально (см. Шаг 3.2)
4. Загрузите файл `digest_bot_session.session` на Railway

### Проблема: "Qdrant not configured"

**Причина:** Qdrant недоступен или неправильно настроен

**Решение:**
1. Проверьте `QDRANT_URL` (должен начинаться с `https://` или `http://`)
2. Проверьте `QDRANT_API_KEY` (для облачного Qdrant обязателен)
3. Проверьте, что кластер Qdrant активен
4. Система продолжит работу без дедупликации (дубликаты не будут фильтроваться)

### Проблема: "No news to create digest"

**Причина:** В буфере нет новостей

**Решение:**
1. Загрузите новости командой `/digest_load 24`
2. Проверьте, что каналы указаны правильно
3. Убедитесь, что в каналах есть сообщения
4. Проверьте права доступа к каналам

### Проблема: "Channel not found" или "No access to channel"

**Причина:** Бот не имеет доступа к каналу

**Решение:**
1. Для публичных каналов: убедитесь, что канал существует и публичен
2. Для приватных каналов: добавьте бота как участника
3. Проверьте правильность написания имени канала (с @ или без)

### Проблема: Ошибки OpenAI

**Причина:** Проблемы с API ключом или балансом

**Решение:**
1. Проверьте баланс на https://platform.openai.com/account/billing
2. Убедитесь, что API ключ правильный
3. Проверьте лимиты использования
4. Убедитесь, что у вас есть доступ к используемым моделям

---

## 📊 Мониторинг работы

### Команды для проверки:

```
/digest_status    # Статус системы
/digest_load 24   # Загрузить новости за 24 часа
/digest_create    # Создать дайджест вручную
```

### Что проверять в логах Railway:

✅ **Успешные сообщения:**
- "✅ Telegram клиент инициализирован"
- "✅ Qdrant коллекция создана"
- "✅ Загружено новостей: X"
- "✅ Дайджест опубликован"

⚠️ **Предупреждения:**
- "⚠️ Qdrant недоступен" - система работает без дедупликации
- "⚠️ Не удалось получить доступ к каналу" - проверьте права

❌ **Ошибки:**
- "❌ Ошибка авторизации Telegram" - проверьте API ключи
- "❌ Ошибка OpenAI" - проверьте API ключ и баланс

---

## 🎉 Готово!

После настройки система будет:
- 📥 Автоматически мониторить каналы в реальном времени
- 🔍 Фильтровать дубликаты
- 📝 Создавать дайджесты по расписанию
- 📤 Публиковать их в указанный канал

**Удачи с настройкой!** 🚀

