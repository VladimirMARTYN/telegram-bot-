# 🛠 Настройка проекта в Xcode

## Создание проекта в Xcode

Поскольку мы создали файлы проекта вручную, вам нужно создать проект в Xcode и добавить туда наши файлы.

### Шаг 1: Создание нового проекта

1. Откройте Xcode
2. Выберите **File → New → Project**
3. Выберите **iOS → App**
4. Нажмите **Next**

### Шаг 2: Настройка проекта

Заполните следующие поля:
- **Product Name**: `FinanceTracker`
- **Team**: Выберите вашу команду (или None для разработки)
- **Organization Identifier**: `com.yourname` (например, `com.ivan`)
- **Bundle Identifier**: будет автоматически заполнен
- **Interface**: **SwiftUI**
- **Language**: **Swift**
- **Storage**: **None** (мы используем UserDefaults)

Нажмите **Next** и выберите папку `FinanceTracker-iOS` для сохранения.

### Шаг 3: Добавление файлов в проект

После создания проекта:

1. Удалите файл `ContentView.swift` который создал Xcode (мы его уже создали)
2. В Xcode нажмите правой кнопкой на папку проекта и выберите **Add Files to "FinanceTracker"...**
3. Выберите папку `FinanceTracker` (со всеми подпапками)
4. Убедитесь, что выбраны опции:
   - ✅ **Copy items if needed** (не выбирайте, если файлы уже в нужной папке)
   - ✅ **Create groups**
   - ✅ **Add to targets: FinanceTracker**

### Шаг 4: Изменение точки входа

1. Откройте файл `FinanceTrackerApp.swift`
2. Убедитесь, что он настроен как точка входа (`@main`)
3. Проверьте, что `ContentView()` используется правильно

### Шаг 5: Настройка Info.plist (опционально)

Файл `Info.plist` обычно создается автоматически. Если нужно настроить:
- Display Name
- Bundle Identifier
- Version

### Шаг 6: Запуск проекта

1. Выберите симулятор (например, iPhone 14 Pro)
2. Нажмите ⌘R или кнопку **Run**
3. Приложение должно запуститься!

## Возможные проблемы

### Ошибка компиляции: "Cannot find type"

Если возникают ошибки о типах:
1. Убедитесь, что все файлы добавлены в проект
2. Проверьте, что все файлы добавлены в target
3. Попробуйте **Product → Clean Build Folder** (⇧⌘K)

### Ошибка с PersistenceController

Если `PersistenceController` вызывает ошибки (так как мы используем UserDefaults):
1. Можете временно закомментировать использование Core Data
2. Или удалить файл `PersistenceController.swift` - он не используется в текущей версии

### SwiftUI Preview не работает

Если Preview не работает:
1. Убедитесь, что используется правильная версия Xcode (14.0+)
2. Попробуйте **Editor → Refresh Canvas** (⌥⌘P)

## Структура файлов в Xcode

После добавления файлов структура должна выглядеть так:

```
FinanceTracker
├── FinanceTrackerApp.swift
├── ContentView.swift
├── Models/
│   ├── Transaction.swift
│   ├── Category.swift
│   └── Budget.swift
├── Views/
│   ├── DashboardView.swift
│   ├── TransactionListView.swift
│   ├── AddTransactionView.swift
│   ├── StatisticsView.swift
│   ├── BudgetView.swift
│   └── SettingsView.swift
├── Services/
│   ├── DataManager.swift
│   └── PersistenceController.swift (опционально)
└── Resources/
    └── (ресурсы, если есть)
```

## Альтернативный способ: Swift Package

Если хотите упростить процесс, можно создать Swift Package:

```bash
cd FinanceTracker-iOS
swift package init --type executable
```

Но для iOS приложения лучше использовать Xcode проект.

## Готово! 🎉

После выполнения этих шагов проект должен компилироваться и запускаться. Если возникнут проблемы - дайте знать!




