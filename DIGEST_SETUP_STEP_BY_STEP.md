# 🎯 Пошаговая настройка AI-дайджестов

## ✅ Шаг 1: Получение Telegram API ключей

1. Откройте https://my.telegram.org/
2. Войдите с номером телефона
3. Перейдите в "API development tools"
4. Создайте приложение
5. Скопируйте `api_id` и `api_hash`

**Запишите здесь:**
- api_id: _______________
- api_hash: _______________

---

## ✅ Шаг 2: Получение Qdrant

### Вариант A: Облачный (рекомендуется)
1. https://cloud.qdrant.io/ → Регистрация
2. Создать бесплатный кластер
3. Скопировать URL и создать API Key

**Запишите здесь:**
- QDRANT_URL: _______________
- QDRANT_API_KEY: _______________

### Вариант B: Локальный
```bash
docker run -p 6333:6333 qdrant/qdrant
```
- URL: `http://localhost:6333`
- API Key: оставить пустым

---

## ✅ Шаг 3: Авторизация в Telegram (ВАЖНО!)

### Проблема
На Railway нет интерактивного ввода, поэтому нужно авторизоваться локально.

### Решение

1. **Создайте локальный `.env` файл:**
```bash
TELEGRAM_API_ID=ваш_id
TELEGRAM_API_HASH=ваш_hash
```

2. **Установите зависимости:**
```bash
pip install telethon python-dotenv
```

3. **Запустите авторизацию:**
```bash
python digest_auth_helper.py
```

4. **Введите:**
   - Номер телефона (+79991234567)
   - Код из Telegram
   - Пароль 2FA (если есть)

5. **Получите файл:** `digest_bot_session.session`

---

## ✅ Шаг 4: Загрузка файла сессии на Railway

### Способ 1: Через Railway CLI (рекомендуется)

```bash
# 1. Войдите в Railway
railway login

# 2. Подключите проект
railway link

# 3. Загрузите файл сессии
railway variables set DIGEST_SESSION_BASE64="$(base64 -i digest_bot_session.session)"
```

Затем нужно будет модифицировать код для чтения из base64.

### Способ 2: Через веб-интерфейс Railway

1. Откройте Railway Dashboard → Variables
2. Добавьте переменную:
   - Имя: `DIGEST_SESSION_BASE64`
   - Значение: содержимое файла в base64

**Как получить base64:**
```bash
base64 -i digest_bot_session.session
```

### Способ 3: Загрузить файл напрямую (если Railway поддерживает)

Railway может поддерживать загрузку файлов через веб-интерфейс или CLI.

---

## ✅ Шаг 5: Настройка переменных на Railway

Добавьте в Railway Dashboard → Variables:

```env
DIGEST_ENABLED=true
TELEGRAM_API_ID=ваш_id
TELEGRAM_API_HASH=ваш_hash
QDRANT_URL=https://ваш-кластер.qdrant.io
QDRANT_API_KEY=ваш_api_key
DIGEST_SOURCE_CHANNELS=@channel1,@channel2
DIGEST_DEST_CHANNEL=@your_digest_channel
DIGEST_PUBLISH_SCHEDULE=daily
```

---

## ✅ Шаг 6: Модификация кода для работы с base64 сессией

Если используете base64, нужно модифицировать `digest_monitor.py` для чтения сессии из переменной окружения.

---

## ✅ Шаг 7: Проверка

После деплоя:
```
/digest_status
/digest_load 24
/digest_create
```

