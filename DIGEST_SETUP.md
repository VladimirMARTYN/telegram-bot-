# 🤖 Настройка AI-дайджестов для Telegram-каналов

## Описание функционала

Система автоматически:
- 📥 Мониторит новости из указанных Telegram-каналов
- 🔍 Находит и удаляет дубликаты с помощью векторного поиска
- 🎯 Группирует новости по темам с помощью AI
- 📝 Создает краткие резюме для каждой темы
- 📤 Публикует структурированные дайджесты по расписанию

## Требования

1. **Telegram API** - получить на https://my.telegram.org/
2. **OpenAI API ключ** - для эмбеддингов и суммаризации
3. **Qdrant** - векторная БД (можно использовать бесплатный tier на https://cloud.qdrant.io/)

## Настройка переменных окружения

Добавьте в ваш `.env` файл:

```env
# Включить систему дайджестов
DIGEST_ENABLED=true

# Telegram API для мониторинга каналов
TELEGRAM_API_ID=your_telegram_api_id
TELEGRAM_API_HASH=your_telegram_api_hash

# Qdrant Vector Database
QDRANT_URL=https://your-instance.qdrant.io
QDRANT_API_KEY=your_qdrant_api_key

# Каналы для мониторинга (через запятую)
DIGEST_SOURCE_CHANNELS=@channel1,@channel2,@channel3

# Канал для публикации дайджестов
DIGEST_DEST_CHANNEL=@your_digest_channel

# Настройки (опционально)
DIGEST_COLLECTION_NAME=telegram_digest
DIGEST_SIMILARITY_THRESHOLD=0.85
DIGEST_EMBEDDING_MODEL=text-embedding-3-small
DIGEST_LLM_MODEL=gpt-4o-mini
DIGEST_PUBLISH_SCHEDULE=daily  # hourly, daily, weekly
```

## Команды бота

### Для администраторов:

- `/digest_status` - показать статус системы дайджестов
- `/digest_create` - создать и опубликовать дайджест вручную
- `/digest_load [часов]` - загрузить последние новости из каналов (по умолчанию 24 часа)

## Расписание публикации

- `hourly` - каждый час
- `daily` - ежедневно в то же время, что и ежедневная сводка
- `weekly` - еженедельно по понедельникам в 09:00

## Пример дайджеста

```
📰 Дайджест новостей
🕐 15.01.2025 14:00

💻 IT (5 новостей)
📝 OpenAI представил новую модель GPT-5 с улучшенными возможностями...

Ключевые новости:
• OpenAI представил GPT-5 с улучшенной производительностью... (@tech_channel)
• Microsoft инвестирует $10 млрд в развитие ИИ... (@tech_channel)
• Google объявил о запуске новой версии Bard... (@tech_channel)

---

💰 ФИНАНСЫ (3 новости)
📝 Центральный банк объявил о новых мерах по регулированию...

Ключевые новости:
• ЦБ РФ ужесточил требования к криптовалютным операциям... (@finance_channel)

---

📊 Всего обработано: 8 новостей
📁 Групп по темам: 2
```

## Первый запуск

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Настройте переменные окружения в `.env`

3. При первом запуске с Telethon вам нужно будет авторизоваться:
   - Введите номер телефона
   - Введите код из Telegram
   - При необходимости введите пароль 2FA

4. Система автоматически:
   - Создаст коллекцию в Qdrant
   - Начнет мониторинг каналов
   - Настроит планировщик публикации

## Устранение проблем

### Qdrant недоступен
- Проверьте URL и API ключ
- Убедитесь, что коллекция создана
- Система продолжит работу без дедупликации

### Telegram клиент не запускается
- Проверьте API_ID и API_HASH
- Убедитесь, что сессия не заблокирована
- Удалите файл `digest_bot_session.session` и перезапустите

### OpenAI ошибки
- Проверьте API ключ
- Убедитесь, что у вас есть доступ к моделям
- Проверьте лимиты использования

## Оптимизация

- Используйте `gpt-4o-mini` для экономии (быстрее и дешевле)
- Настройте `DIGEST_SIMILARITY_THRESHOLD` для более/менее строгой фильтрации дубликатов
- Используйте `hourly` для более частых дайджестов
- Ограничьте количество каналов для мониторинга

