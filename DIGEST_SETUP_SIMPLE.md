# 🚀 Простая настройка AI-дайджестов (пошагово)

## 📋 Что нужно сделать

### Шаг 1: Получить Telegram API ключи (5 минут)

1. Откройте https://my.telegram.org/
2. Войдите с номером телефона
3. Перейдите в **"API development tools"**
4. Создайте приложение (любое название)
5. Скопируйте:
   - **api_id** (число)
   - **api_hash** (строка)

---

### Шаг 2: Получить Qdrant (5 минут)

**Вариант A: Облачный (рекомендуется)**
1. Зарегистрируйтесь на https://cloud.qdrant.io/
2. Создайте бесплатный кластер
3. Скопируйте **URL** и создайте **API Key**

**Вариант B: Локальный (для тестирования)**
```bash
docker run -p 6333:6333 qdrant/qdrant
```
URL: `http://localhost:6333`, API Key не нужен

---

### Шаг 3: Авторизация в Telegram (важно!)

**Проблема:** На Railway нет интерактивного ввода для авторизации.

**Решение:** Авторизуйтесь локально и загрузите файл сессии.

#### 3.1 Локальная авторизация

1. **Создайте локальный `.env` файл:**
```bash
TELEGRAM_API_ID=ваш_id
TELEGRAM_API_HASH=ваш_hash
```

2. **Установите зависимости:**
```bash
pip install telethon python-dotenv
```

3. **Запустите скрипт авторизации:**
```bash
python digest_auth_helper.py
```

4. **Введите данные:**
   - Номер телефона (например: +79991234567)
   - Код из Telegram
   - Пароль 2FA (если включен)

5. **Получите файл:** `digest_bot_session.session`

#### 3.2 Загрузка файла на Railway

**Способ 1: Через Railway CLI**
```bash
railway login
railway link  # подключите проект
railway variables set DIGEST_SESSION_FILE=@digest_bot_session.session
```

**Способ 2: Через веб-интерфейс Railway**
1. Откройте Railway Dashboard → Variables
2. Добавьте переменную типа "File" (если доступно)
3. Загрузите файл `digest_bot_session.session`

**Способ 3: Через base64 (если файлы не поддерживаются)**
```bash
# Конвертируйте файл в base64
base64 -i digest_bot_session.session > session_base64.txt

# Добавьте в Railway как переменную
railway variables set DIGEST_SESSION_BASE64="<содержимое session_base64.txt>"
```

Затем нужно будет модифицировать код для чтения из base64.

---

### Шаг 4: Настройка переменных на Railway

Добавьте в Railway Dashboard → Variables:

```env
# Включить систему
DIGEST_ENABLED=true

# Telegram API (из шага 1)
TELEGRAM_API_ID=ваш_id
TELEGRAM_API_HASH=ваш_hash

# Qdrant (из шага 2)
QDRANT_URL=https://ваш-кластер.qdrant.io
QDRANT_API_KEY=ваш_api_key

# Каналы для мониторинга (через запятую)
DIGEST_SOURCE_CHANNELS=@channel1,@channel2

# Канал для публикации
DIGEST_DEST_CHANNEL=@your_digest_channel

# Расписание (daily/hourly/weekly)
DIGEST_PUBLISH_SCHEDULE=daily
```

---

### Шаг 5: Проверка

После деплоя выполните в боте:

```
/digest_status    # Проверить статус
/digest_load 24   # Загрузить новости
/digest_create    # Создать тестовый дайджест
```

---

## ⚠️ Важные моменты

1. **Файл сессии** - содержит ваши учетные данные, храните в безопасности
2. **Права доступа** - бот должен иметь доступ к каналам для мониторинга
3. **Канал публикации** - бот должен быть администратором канала
4. **Расходы OpenAI** - ~$0.003/день при 100 новостях

---

## 🆘 Если что-то не работает

### "Telegram client not initialized"
- Проверьте `TELEGRAM_API_ID` и `TELEGRAM_API_HASH`
- Убедитесь, что файл сессии загружен на Railway
- Попробуйте авторизоваться заново локально

### "Qdrant not configured"
- Проверьте `QDRANT_URL` и `QDRANT_API_KEY`
- Система продолжит работу без дедупликации

### "No news to create digest"
- Выполните `/digest_load 24`
- Проверьте, что каналы указаны правильно

