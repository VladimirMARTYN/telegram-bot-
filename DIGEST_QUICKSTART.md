# 🚀 Быстрый старт: Запуск AI-дайджестов

## Шаг 1: Установка зависимостей

```bash
pip install telethon qdrant-client openai
```

Или обновите все зависимости:
```bash
pip install -r requirements.txt
```

## Шаг 2: Получение API ключей

### 2.1 Telegram API (для мониторинга каналов)

1. Перейдите на https://my.telegram.org/
2. Войдите с вашим номером телефона
3. Перейдите в раздел "API development tools"
4. Создайте приложение (любое название)
5. Скопируйте:
   - **api_id** (число)
   - **api_hash** (строка)

### 2.2 Qdrant (векторная БД для поиска дубликатов)

**Вариант 1: Бесплатный облачный Qdrant**
1. Зарегистрируйтесь на https://cloud.qdrant.io/
2. Создайте бесплатный кластер
3. Скопируйте:
   - **URL** (например: `https://xxxxx.qdrant.io`)
   - **API Key** (из настроек кластера)

**Вариант 2: Локальный Qdrant (для тестирования)**
```bash
docker run -p 6333:6333 qdrant/qdrant
```
URL будет: `http://localhost:6333`
API Key не нужен (оставьте пустым)

### 2.3 OpenAI API (уже должен быть)

Проверьте, что у вас есть `OPENAI_API_KEY` в `.env`

## Шаг 3: Настройка переменных окружения

Откройте файл `.env` и добавьте/обновите следующие переменные:

```env
# Включить систему дайджестов
DIGEST_ENABLED=true

# Telegram API (из шага 2.1)
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=abcdef1234567890abcdef1234567890

# Qdrant (из шага 2.2)
QDRANT_URL=https://your-instance.qdrant.io
QDRANT_API_KEY=your_qdrant_api_key

# Каналы для мониторинга (укажите реальные каналы)
DIGEST_SOURCE_CHANNELS=@channel1,@channel2,@channel3

# Канал для публикации дайджестов (ваш канал или бот)
DIGEST_DEST_CHANNEL=@your_digest_channel

# Расписание публикации (hourly/daily/weekly)
DIGEST_PUBLISH_SCHEDULE=daily
```

## Шаг 4: Первый запуск

### 4.1 Запустите бота

```bash
python admin_bot.py
```

### 4.2 Авторизация в Telegram

При первом запуске Telethon попросит авторизоваться:
1. Введите ваш номер телефона (с кодом страны, например: +79991234567)
2. Введите код из Telegram
3. Если включена 2FA, введите пароль

После авторизации создастся файл `digest_bot_session.session` - не удаляйте его!

### 4.3 Проверка статуса

В боте выполните команду (только для админа):
```
/digest_status
```

Должно показать:
- ✅ Система включена
- Список каналов для мониторинга
- Канал публикации
- Статус буфера

## Шаг 5: Тестирование

### 5.1 Загрузить последние новости

```
/digest_load 24
```

Это загрузит новости за последние 24 часа из указанных каналов.

### 5.2 Создать дайджест вручную

```
/digest_create
```

Это создаст и опубликует дайджест из накопленных новостей.

## Шаг 6: Автоматическая работа

После настройки система будет:
- 📥 Автоматически мониторить каналы в реальном времени
- 🔍 Фильтровать дубликаты
- 📝 Создавать дайджесты по расписанию
- 📤 Публиковать их в указанный канал

## ⚠️ Важные замечания

1. **Права доступа к каналам**: 
   - Бот должен иметь доступ к каналам для мониторинга
   - Для публичных каналов достаточно подписки
   - Для приватных каналов нужно добавить бота как участника

2. **Канал публикации**:
   - Может быть ваш личный канал или бот
   - Если указан бот, дайджест отправится в личные сообщения
   - Для канала бот должен быть администратором

3. **Расходы OpenAI**:
   - Каждое сообщение создает эмбеддинг (~$0.0001)
   - Суммаризация использует GPT-4o-mini (~$0.001 за дайджест)
   - При большом количестве новостей расходы могут быть значительными

## 🔧 Настройка расписания

- `hourly` - каждый час (для активных каналов)
- `daily` - ежедневно в 09:00 (по умолчанию)
- `weekly` - еженедельно по понедельникам

## 📊 Мониторинг работы

Проверяйте логи бота на наличие:
- ✅ Успешных сообщений о загрузке новостей
- ⚠️ Предупреждений о недоступности API
- ❌ Ошибок обработки

## 🆘 Решение проблем

### Ошибка "Telegram client not initialized"
- Проверьте `TELEGRAM_API_ID` и `TELEGRAM_API_HASH`
- Удалите `digest_bot_session.session` и перезапустите

### Ошибка "Qdrant not configured"
- Проверьте `QDRANT_URL` и `QDRANT_API_KEY`
- Система продолжит работу без дедупликации

### "No news to create digest"
- Загрузите новости командой `/digest_load`
- Проверьте, что каналы указаны правильно
- Убедитесь, что в каналах есть сообщения

### Ошибки OpenAI
- Проверьте баланс на счету OpenAI
- Убедитесь, что API ключ правильный
- Проверьте лимиты использования

---

**Готово!** Система дайджестов должна работать. 🎉

